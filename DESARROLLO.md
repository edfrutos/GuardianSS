# Estado de desarrollo — GuardianSS

Notas de la revisión del repo `macos-app/GuardianSS`, a fecha 2026-08-02. Complementa a `README.md` (arquitectura y uso) con el diagnóstico de dónde está el proyecto y qué falta.

## Qué es

GuardianSS es la interfaz nativa macOS (SwiftUI) del proyecto Guardian Secret Scanner. No contiene lógica de detección propia: delega el escaneo real en el script Python `scan_sensitive.py` (fuera de este repo, en `secret-scanner-tool/`), lo lanza como subproceso y pinta sus resultados JSON.

## Funcionalidad en desarrollo

- Selección de carpeta vía `NSOpenPanel` y lanzamiento de escaneo (`ScannerManager.runScan`).
- Listado de archivos con alertas, detalle por archivo (tipo de alerta, línea, fragmento detectado) y vista de selección múltiple.
- Cuarentena: individual (`quarantineFile`) y por lote (`quarantineFiles` / `quarantineFileSync`), con lectura de metadatos de traslado (`.metadata.json`) para mostrar fecha y ruta original. Modo copia (conserva el original) o mover (lo elimina), y directorio raíz de cuarentena configurable (por defecto `quarantine/` junto al script, con subcarpeta por fecha).
- Restauración desde cuarentena: individual (`restoreFile`) y por lote (`restoreFiles`), devuelve un archivo aislado a la carpeta exacta de la que se cogió (leída de `.metadata.json`), sin importar si la cuarentena usó el directorio por defecto o uno indicado por el usuario.
- Hardened Runtime activado vía entitlements; App Sandbox desactivado a nivel de proyecto (`ENABLE_APP_SANDBOX = NO`), necesario porque la app lanza procesos externos y accede a rutas arbitrarias elegidas por el usuario.

## Lo que se lleva hecho (esta sesión)

1. **Sincronización del repo**: se ordenó un working tree con archivos duplicados y a medio-stage; se limpiaron dos copias obsoletas de `ScannerLogic.swift`, se creó `.gitignore` (excluye `.gemini/`, `GEMINI.md`, el binario `GuardianSSTests` y `.DS_Store`) y se hizo el primer commit real de la lógica de escaneo, entitlements endurecidos y tests.
2. **Revisión de datos sensibles**: sin credenciales ni secretos reales en el código. Hallazgos de bajo riesgo documentados (ruta hardcodeada del script, Apple Team ID en `project.pbxproj`).
3. **README.md**: creado desde cero para este subproyecto (no existía); cubre arquitectura, requisitos, build/run y notas de seguridad.
4. **Refactor de robustez**: la ruta al script Python y la búsqueda del ejecutable `python3` estaban duplicadas idénticas en 4 sitios (`runScan`, `quarantineFile`, `quarantineFiles`→`quarantineFileSync`). Se centralizaron en `ScannerManager.scriptPath` (con override por `GUARDIANSS_SCAN_SCRIPT`) y `resolvePythonExecutable()`. Ahora, si el script no existe en la ruta resuelta, se muestra un error explícito en vez de fallar en silencio o lanzar un proceso condenado a fallar. Verificado con `xcodebuild build` → `BUILD SUCCEEDED`.
5. **Migración de tests a XCTest real**: se sustituyó el arnés casero (`@main struct TestRunner`) por un target `GuardianSSTests` de verdad (`com.apple.product-type.bundle.unit-test`), añadido al `.pbxproj` con la gem `xcodeproj` y registrado en el scheme compartido. Los 4 tests se movieron a `GuardianSSTests/GuardianSSTests.swift` con `XCTAssert*`. Hubo que igualar `ARCHS = arm64e` en el nuevo target (la app compila en `arm64e` por el hardened runtime; el target de test por defecto se creaba en `arm64`, y un `.xctest` no puede cargarse por `dlopen` en un host de arquitectura distinta). Verificado con `xcodebuild test` → **TEST SUCCEEDED**, 4/4 pasan.
6. **Copiar en vez de mover**: nueva funcionalidad completa, motor + app.
   - `scan_sensitive.py` (fuera de este repo, en `secret-scanner-tool/`): `move_file` renombrado a `quarantine_file`, ahora acepta `mode` ('move'/'copy'/None) en vez de un booleano. CLI: `--move` y `--copy` son mutuamente excluyentes (`argparse.add_mutually_exclusive_group`); `--move-to <ruta>` (ya existía) define el directorio raíz, con subcarpeta por fecha dentro — ese ya era el comportamiento "por defecto `quarantine/` salvo que se elija otro" que pediste. El JSON de resultados gana el campo `copiado: bool`, y `.metadata.json` gana `modo`. Probado manualmente: copia dentro de directorio custom con original intacto, y modo mover sigue igual que antes (regresión).
   - `ScannerLogic.swift`: `ScanResult` gana `copiado: Bool?`, `FileMetadata` gana `modo: String?`. `ScannerManager` gana `@Published var copyInsteadOfMove` y `@Published var customQuarantineDir: String?`, más un helper `quarantineArgs()` que construye `--copy`/`--move` (+ `--move-to` si hay dir custom) — usado en los 3 puntos que antes tenían `--move` hardcodeado (`runScan`, `quarantineFile`, `quarantineFileSync`).
   - `ContentView.swift`: toggle "Copiar en vez de mover" (solo visible si la cuarentena al escanear está activa), componente nuevo `QuarantineDirectoryPicker` (selector de carpeta + botón para volver al valor por defecto), y las etiquetas/botones ("Aislado" → "Copiado", "Aislar archivo" → "Copiar a cuarentena", aviso de que el original permanece) se adaptan al modo activo.
   - `GuardianSSTests.swift`: actualizado para el nuevo parámetro `copiado` en el init memberwise de `ScanResult`. Verificado con `xcodebuild test` → 4/4 pasan.
7. **Icono real**: `AppIcon.appiconset` poblado con las 10 imágenes generadas desde el arte proporcionado (`sips`); se eliminó el mecanismo legacy (`icon.icns` + `INFOPLIST_KEY_CFBundleIconFile`), quedando solo `ASSETCATALOG_COMPILER_APPICON_NAME` (que ya estaba puesto pero sin catálogo real detrás).
8. **Rediseño UI/UX** (`GuardianTheme.swift` + `ContentView.swift`): paleta ámbar/azul adaptativa inspirada en el icono, tarjetas de cristal, insignias con halo, chips de severidad. Verificado interactuando con la app real compilada (capturas de pantalla vía `screencapture -l<windowID>` + clics simulados con `CGEvent`, ver más abajo). Esa verificación encontró y corrigió en el momento:
   - Sidebar colapsado a icono-only por falta de `navigationSplitViewColumnWidth`.
   - Contenido cortado en ventana pequeña por falta de `ScrollView` en las vistas de estado.
   - Toggle "Copiar en vez de mover" oculto sin necesidad tras "Cuarentena al escanear", pese a afectar también al botón manual.
   - Barra de pestañas nativa de macOS duplicando el título (deshabilitada con `NSWindow.allowsAutomaticWindowTabbing = false`).
   - **Bug real**: `ScanResult.==`/`hash` solo comparaban `archivo`, así que tras aislar/copiar un archivo la fila del sidebar no se refrescaba (SwiftUI la creía "sin cambios"). Se quitó la implementación custom para que Swift sintetice igualdad por contenido completo; `Identifiable.id` sigue siendo `archivo`. El test que codificaba el comportamiento antiguo como correcto se reescribió.
9. **Repositorio GitHub y releases**: creado `github.com/edfrutos/GuardianSS` (privado) y empujado todo el historial. Se añadió `scripts/release.sh` (build Release → DMG vía `hdiutil` → tag `vX.Y` → `gh release create`) y se publicó la primera release (`v1.0`).
10. **Comprobación de actualizaciones** (`UpdateChecker.swift`): compara `MARKETING_VERSION` contra la última release de GitHub. Como el repo es privado, no se llama a la API HTTP directamente (exigiría embeber un token en el binario); se invoca `gh release view` como subproceso, igual que ya se hace con `python3`. Aviso discreto en el sidebar si hay versión nueva, más botón manual en la toolbar. Verificado en la app real: con la versión instalada igual a la última release, correctamente no muestra aviso.
11. **Arrastrar y soltar carpeta** (`ContentView.swift`): `.onDrop(of: [.fileURL])` en la vista raíz, funciona en cualquier estado de la app y llama al mismo `ScannerManager.runScan` que el botón "Escanear Carpeta" — sin duplicar lógica. Overlay con borde discontinuo ámbar mientras se arrastra encima; si se suelta un archivo (no carpeta), reutiliza el estado de error existente. Verificado por ti arrastrando una carpeta real.
12. **Botón de actualizaciones poco descubrible + sin feedback**: reportaste que "no lo veías". El icono (↻ en la toolbar) sí existía, pero sin etiqueta y sin dar ninguna señal visible si no había actualización — parecía no hacer nada. Se añadió la entrada de menú estándar de macOS **GuardianSS → Buscar actualizaciones...** (patrón Sparkle), y ahora las comprobaciones manuales (menú o botón) siempre muestran una alerta con el resultado, aunque sea "ya tienes la última versión". `UpdateChecker` pasó a vivir en `GuardianSSApp` (antes en `ContentView`) y se comparte vía `.environmentObject` para que menú y toolbar actúen sobre la misma instancia.
    - **Bug real encontrado y arreglado durante la verificación**: la alerta se fijaba en el estado (`manualCheckNotice` no-nil confirmado por logs) pero no se veía en pantalla. Causa: se derivaba `isPresented` con `.onChange(of: updateChecker.algunaPropiedad)` — patrón frágil en SwiftUI cuando el valor observado es una propiedad de un `ObservableObject` externo, ya que el valor "vuelve" a su estado inicial dentro del mismo ciclo de actualización y la alerta nunca llega a pintarse. Solución: un `@Published var showManualCheckAlert` dentro de `UpdateChecker`, enlazado directamente desde la vista con `$updateChecker.showManualCheckAlert` (binding nativo del `@EnvironmentObject`, sin capa intermedia). Verificado end-to-end contra la app real compilada.
13. **Repositorio pasado a público (2026-08-14)**: auditado el historial completo de git (patrones de credenciales, nombres de fichero sensibles, tokens embebidos) sin hallar material sensible real — solo hallazgos de baja severidad ya conocidos (Team ID en `project.pbxproj`, username en `xcschememanagement.plist`). Con el repo ya público, `UpdateChecker.swift` se migró de invocar `gh release view` (subproceso, requería `gh` CLI instalado y logueado en la máquina del usuario) a una llamada HTTP anónima directa a `GET https://api.github.com/repos/edfrutos/GuardianSS/releases/latest` vía `URLSession`. Elimina la dependencia de `gh` CLI para el usuario final; `scripts/release.sh` sigue usando `gh release create` porque esa parte solo la ejecuta el desarrollador al publicar, no cambia con la visibilidad del repo.
14. **Documentación del proceso de release (`RELEASING.md`, nuevo)**: no existía ningún `.md` para desarrolladores con el paso a paso operativo de cortar una release (solo estaba descrito qué hace `scripts/release.sh`, no el flujo de git alrededor). Cubre también las particularidades de publicar ayudándose de un agente en un sandbox de Claude Code (sin `xcodebuild`/`hdiutil`, modo de workspace directo vs. clon, credenciales de `git push`).
15. **Firma Developer ID + notarización en `scripts/release.sh`**: la release `v1.2` recién publicada (firmada solo con certificado *Apple Development*, sin notarizar) disparaba en Gatekeeper "Apple no ha podido verificar que GuardianSS.app no contenga software malicioso..." al abrirla — el pendiente que ya estaba anotado desde antes de hacer el repo público. Como sí tienes cuenta de pago de Apple Developer Program, se cambió el script: `xcodebuild archive` + `-exportArchive` con `method: developer-id` (re-firma con el certificado Developer ID del team en vez del de depuración), `xcrun notarytool submit --wait` contra un perfil de keychain (`GuardianSS-notary`), y `xcrun stapler staple` tanto al `.app` como al `.dmg` final. Requiere setup único documentado en `RELEASING.md` (certificado Developer ID generado una vez desde Xcode, credenciales de notarización guardadas en el keychain). Publicada como `v1.3` (se saltó `v1.2`, que quedó rota y sin republicar) tras corregir además un fallo transitorio del grapado del DMG (CloudKit tarda en propagar el ticket para un segundo artefacto justo después del primero — ahora reintenta una vez y, si sigue fallando, avisa pero no aborta la release, porque el `.app` de dentro ya está grapado, que es lo único que Gatekeeper comprueba al ejecutarlo).
16. **Descarga e instalación automática de actualizaciones** (`UpdateChecker.swift`): a petición tuya, dejó de ser solo un aviso con enlace — ahora, en cuanto detecta versión nueva, descarga sola el DMG adjunto a la release en segundo plano (con progreso en el banner del sidebar), lo monta, y antes de instalar nada verifica `codesign --verify --deep --strict`, `spctl --assess --type execute` y que el `TeamIdentifier` de la firma coincida con `V29BTBRY6G` (constante `expectedTeamID`, hardcodeada igual que `repo`) — si cualquiera de esas comprobaciones falla, no se instala y el banner muestra el error con un enlace de vuelta a la página de la release. Con la actualización verificada y copiada a un `.app` temporal, el banner pasa a "Reiniciar y actualizar" / "Más tarde" (decisión tuya: nunca se instala sin que pulses el botón, para no interrumpir un escaneo o cuarentena en curso). Probado en real: bajando `MARKETING_VERSION` a 1.2 localmente y corriendo desde Xcode, detectó, descargó, verificó e instaló la v1.3 real sola, relanzándose ya como v1.3.
17. **Bug encontrado en la propia prueba de la 1.2→1.3**: el chequeo manual ("Buscar actualizaciones...") no mostraba ninguna alerta cuando ya había una actualización detectada/descargándose — solo alertaba en el caso "ya tienes la última versión", pese a que la intención original (documentada en el punto 12) era que el chequeo manual "siempre" diera feedback. Con la descarga automática de por medio esto se notó enseguida ("el menú no hace nada"). Se arregló para que también informe cuando sí hay actualización disponible.
18. **Ubicación de instalación configurable, primer lanzamiento** (`InstallLocation.swift`, nuevo): pediste que la primera vez se pudiera elegir el directorio de destino (con `/Applications` como opción por defecto), y poder cambiarlo después si no interesa el de por defecto. El motivo real detrás, más allá de la comodidad: el instalador de actualizaciones apuntaba a `Bundle.main.bundlePath` — es decir, "donde sea que esta instancia esté corriendo ahora mismo", que puede ser un DMG montado, Descargas, o (como pasó en las pruebas) una carpeta de DerivedData de Xcode. Ahora `InstallLocation` persiste en `UserDefaults` una carpeta de destino estable (`/Applications` por defecto), `InstallSetupManager` pregunta por ella en el primer lanzamiento de una build de Release (`#if !DEBUG` — no molesta durante desarrollo con ⌘R) con alerta de 3 vías (usar la de por defecto / elegir otra carpeta con `NSOpenPanel.begin` async, siguiendo el mismo patrón que el resto de selectores de carpeta de la app / posponer sin marcar como completado), y hay un ajuste "Ubicación de instalación" al pie del sidebar para cambiarla más tarde. `UpdateChecker.installAndRelaunch()` se simplificó para reutilizar la lógica de sustituir-y-relanzar (`RelaunchHelper`, extraída de lo que antes era código duplicado inline) apuntando siempre a `InstallLocation.appPath` en vez de a donde esté corriendo la instancia actual — así una actualización aterriza siempre en el sitio correcto.
19. **Auditoría de deuda pendiente y refuerzo de tests (2026-08-16)**: revisión de los puntos que este documento listaba como abiertos, contrastados contra el código y el git log actuales. Tres de ellos (feedback de errores en cuarentena por lote, `xcschememanagement.plist` versionado, duplicación en `MultiDetailView`) ya estaban resueltos desde `acb7404` (2026-08-04) y no se habían reflejado aquí — corregido. Sobre el punto real que seguía abierto (cobertura de tests): se extrajo el filtrado de líneas `[*]` + JSON de la salida del subproceso, duplicado de forma idéntica en `runScan`, `quarantineFile` y `quarantineFileSync`, a una función pura `ScannerManager.extractJSONPayload(from:)`; y `quarantineArgs()` pasó de `private` a interno para poder testearse directamente. Se añadieron 8 tests nuevos (`GuardianSSTests.swift`) cubriendo `quarantineArgs()` (modo mover/copiar, directorio custom, combinaciones, directorio vacío ignorado) y `extractJSONPayload` (filtrado de logs, passthrough sin logs, salida vacía). Sigue sin cubrirse el propio lanzamiento de `Process` en `runScan`/`quarantineFile` (requeriría inyectar un runner mockeable). No se pudo compilar/ejecutar `xcodebuild test` en esta sesión por no haber toolchain de macOS en el sandbox usado — pendiente de verificación en Xcode real.
20. **Restauración desde cuarentena (2026-08-17)**: pediste poder devolver a su carpeta original un archivo que se había aislado (movido o copiado) al directorio de cuarentena por defecto de la app o a uno indicado por el usuario.
    - `scan_sensitive.py` (fuera de este repo, en `secret-scanner-tool/`): nueva función `restore_file(quarantine_path, force=False, restore_to=None)` y modo CLI `--restore <ruta_en_cuarentena>`. Lee el `.metadata.json` que ya se escribía junto a cada archivo aislado, mueve el archivo de vuelta a su `original_path` (o a `--restore-to` si se indica uno distinto), y borra el metadato tras el éxito. Si ya existe un archivo en el destino, no lo sobreescribe salvo `--force` — evita perder trabajo por una restauración accidental. `directorio` pasó de argumento posicional obligatorio a opcional (`nargs="?"`), validado a mano solo cuando no se usa `--restore`. Probado manualmente con un roundtrip real (mover a cuarentena → restaurar → reintentar restaurar el mismo archivo, que falla correctamente al no encontrarlo ya en cuarentena).
    - `ScannerLogic.swift`: `RestoreResult` (modelo del JSON de `--restore`), `@Published var isRestoring`, y `restoreFile(quarantinePath:completion:)` / `restoreFiles(quarantinePaths:)` — mismo patrón que `quarantineFile`/`quarantineFiles` (lanzan el subproceso Python, parsean el JSON con `extractJSONPayload`). Al restaurar con éxito, el resultado se quita de `results` porque el archivo ha dejado de estar en cuarentena.
    - `ContentView.swift`: botón **"Restaurar"** en la tarjeta de estado de `DetailView` (con `confirmationDialog` mostrando a qué carpeta va a volver, porque mover un archivo fuera de cuarentena sin avisar es una acción destructiva si hay algo distinto ya en esa ruta), y **"Restaurar N a su carpeta original"** en `MultiDetailView` para selección múltiple.
    - De paso, arreglada la ruta por defecto de `GUARDIANSS_SCAN_SCRIPT` (desactualizada desde que el repo padre se movió a `__01.-Github_Repositories/`) y quitados `SWIFT_APPROACHABLE_CONCURRENCY`/`SWIFT_DEFAULT_ACTOR_ISOLATION` del target de la app: solo generaban ruido de chequeo de `Sendable` contra frameworks del sistema (un aviso sobre `IOCollector`, símbolo que no existe en ningún fichero de este repo), sin beneficio funcional. No se pudo compilar/ejecutar en Xcode real desde este sandbox (sin toolchain de macOS); confirmaste en Xcode real que `BUILD SUCCEEDED` y que el escaneo funciona — aún sin confirmar el flujo mover→restaurar de extremo a extremo en la app compilada.

## Errores / problemas detectados (histórico, todos resueltos)

| Severidad | Problema | Resuelto en |
|---|---|---|
| Baja | Estado de usuario versionado | `xcschememanagement.plist` desatrackeado en `acb7404`. |
| Baja | Cuarentena por lote sin feedback de error | `quarantineFileSync` devuelve `Bool` y `quarantineFiles` acumula fallos en `errorMessage` desde `acb7404`. |
| Baja | Duplicación menor en `MultiDetailView` | Ya no existe; el componente solo calcula `unisolatedPaths` una vez. |

No se han encontrado bugs de severidad alta ni problemas de seguridad explotables (credenciales, inyección de comandos — `Process.arguments` se usa como array, no hay shell interpolation).

## Pendiente (revisado 2026-08-17)

- **Cobertura de tests**: antes solo cubrían modelos, estado inicial y comparación de versiones; faltaba el parseo de la salida real del subproceso y la construcción de argumentos de cuarentena. Se cerró parcialmente en la sesión del 2026-08-16 (ver punto 19) extrayendo `extractJSONPayload` y haciendo `quarantineArgs()` testable. Sigue faltando cobertura de `restoreFile`/`restoreFiles` (punto 20) y del lanzamiento de `Process` en general en `runScan`/`quarantineFile` (requeriría inyectar un runner mockeable en vez de instanciar `Process` directamente).
- Verificar en Xcode real el flujo completo de restauración (mover a cuarentena → "Restaurar" en la UI → el archivo vuelve a la carpeta original) — solo probado por CLI (`scan_sensitive.py --restore`) desde el sandbox, sin toolchain de macOS disponible ahí para compilar la app.
- El README de la carpeta padre (`secret-scanner-tool/README.md`) sí describía un flujo de instalación desactualizado (ruta del repo previa a moverse a `__01.-Github_Repositories/`) — **resuelto el 2026-08-17**, junto con el mismo arreglo en este README y en `GUARDIANSS_SCAN_SCRIPT`.
- Notarización y firma Developer ID: **ya resuelto** (`v1.3`, commits `15a3074`/`3ce1611`), se deja de listar aquí a partir de esta revisión.
